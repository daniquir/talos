# --- Rust & Compilation ---
**/target/
**/*.rs.bk
Cargo.lock

# --- Docker & Logs ---
.env
*.log
docker-compose.override.yml

# --- System Files / IDE ---
.DS_Store
.idea/
.vscode/
*.swp

# --- Security (CRITICAL!) ---
# Never upload your keys or password database if they are inside the project folder
# Although we map them from HOME (~/), just in case you copy them here:
.gnupg/
.password-store/